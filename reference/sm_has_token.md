# Check if a token is available

Checks if a token is present in the internal authentication state. This
is an internal helper function.

## Usage

``` r
sm_has_token()
```

## Value

Logical. `TRUE` if a token is available, `FALSE` otherwise.
