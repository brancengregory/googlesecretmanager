% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secrets.R
\name{create_secret}
\alias{create_secret}
\title{Create a Google Cloud Secret}
\usage{
create_secret(
  project_id,
  secret_id,
  labels = NULL,
  replication_policy = NULL,
  etag = NULL,
  ttl = NULL
)
}
\arguments{
\item{project_id}{The Google Cloud Project ID.}

\item{secret_id}{The ID of the secret to create. Must be unique within the project.}

\item{labels}{Optional. A list of key-value pairs to attach as labels to the secret.}

\item{replication_policy}{Optional. A list defining the replication policy.
For example, \code{list(automatic = list())} for automatic replication, or
\code{list(userManaged = list(replicas = list(list(location = "us-east1"))))}
for user-managed replication in "us-east1".}

\item{etag}{Optional. For optimistic concurrency control.}

\item{ttl}{Optional. The TTL (Time-To-Live) of the secret. Must be a string
representing a duration, e.g., "3600s" for 1 hour.}
}
\value{
A list containing information about the newly created secret.
}
\description{
Creates a new secret in the specified Google Cloud Project.
}
\examples{
\dontrun{
secret_info <- create_secret(
  project_id = "your-gcp-project-id",
  secret_id = "my-new-secret",
  labels = list(environment = "development"),
  replication_policy = list(automatic = list())
)
print(secret_info)
}
}
